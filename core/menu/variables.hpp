#pragma once

namespace variables {
	inline bool blink_switch = true;

	namespace visuals {
		inline bool player_esp = true;
		inline bool player_esp_dead_only = false;
	};

	namespace movement {
		inline bool bunnyhop = true;
	};

	namespace fun {
		inline bool trashtalk = false;
	};

	namespace menu {
		inline bool is_open = false;
	};
}